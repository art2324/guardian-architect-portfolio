# Nginx Web Server

## Purpose
Serve a basic HTTP webpage to validate VM deployment.

## Installation
Nginx was installed via package manager on Ubuntu.

## Validation
Default Nginx welcome page is accessible via public IP over HTTP (port 80).

## Status
Service running and accessible externally.